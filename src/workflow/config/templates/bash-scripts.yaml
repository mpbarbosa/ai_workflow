# Bash/Shell Project Configuration Template
# Auto-generated by AI Workflow Automation
# Version: 1.0.0

_template_info:
  name: "Bash/Shell Scripts"
  description: "Bash/Shell scripting project"
  language: "bash"
  build_system: "none"
  version: "1.0.0"

project:
  name: "my-bash-project"
  description: "A Bash/Shell script project"

tech_stack:
  primary_language: "bash"
  languages:
    - bash
  build_system: "none"
  test_framework: "bats"
  linter: "shellcheck"
  test_command: "bats tests/"
  lint_command: "shellcheck *.sh"

structure:
  source_dirs:
    - lib
    - bin
  test_dirs:
    - tests
  docs_dirs:
    - docs
  exclude_dirs: []

dependencies:
  package_file: ""
  install_command: "echo 'No installation needed'"

ai_prompts:
  language_context: |
    This is a Bash/Shell scripting project.
    Focus on portability, safety, and best practices.
  custom_instructions:
    - "Use set -euo pipefail for safety"
    - "Quote all variable expansions"
    - "Use [[ ]] for conditionals"
    - "Document functions clearly"
