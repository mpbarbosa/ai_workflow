# Python Project Configuration Template (pip)
# Auto-generated by AI Workflow Automation
# Version: 1.0.0

_template_info:
  name: "Python with pip"
  description: "Python project using pip for dependency management"
  language: "python"
  build_system: "pip"
  version: "1.0.0"

project:
  name: "my-python-project"
  description: "A Python project"

tech_stack:
  primary_language: "python"
  languages:
    - python
  build_system: "pip"
  test_framework: "pytest"
  linter: "pylint"
  test_command: "pytest"
  lint_command: "pylint src/"

structure:
  source_dirs:
    - src
  test_dirs:
    - tests
  docs_dirs:
    - docs
  exclude_dirs:
    - venv
    - .venv
    - __pycache__
    - .pytest_cache
    - dist
    - build
    - "*.egg-info"

dependencies:
  package_file: "requirements.txt"
  lock_file: "requirements.lock"
  install_command: "pip install -r requirements.txt"

ai_prompts:
  language_context: |
    This is a Python project using pip for dependency management.
    Focus on Python best practices and PEP standards.
  custom_instructions:
    - "Follow PEP 8 style guide"
    - "Use type hints (PEP 484)"
    - "Write comprehensive docstrings (PEP 257)"
    - "Document exceptions with raises sections"
