# C/C++ Project Configuration Template (CMake)
# Auto-generated by AI Workflow Automation
# Version: 1.0.0

_template_info:
  name: "C/C++ with CMake"
  description: "C/C++ project using CMake for build management"
  language: "cpp"
  build_system: "cmake"
  version: "1.0.0"

project:
  name: "my-cpp-project"
  description: "A C/C++ project"

tech_stack:
  primary_language: "cpp"
  languages:
    - cpp
    - c
  build_system: "cmake"
  test_framework: "gtest"
  linter: "clang-tidy"
  test_command: "ctest --test-dir build"
  lint_command: "clang-tidy src/*.cpp"

structure:
  source_dirs:
    - src
    - include
  test_dirs:
    - tests
  docs_dirs:
    - docs
  exclude_dirs:
    - build
    - cmake-build-debug
    - .deps

dependencies:
  package_file: "CMakeLists.txt"
  install_command: "cmake -B build && cmake --build build"

ai_prompts:
  language_context: |
    This is a C/C++ project using CMake.
    Focus on modern C++ practices and safety.
  custom_instructions:
    - "Use modern C++ features (C++11+)"
    - "Follow RAII principles"
    - "Document complex algorithms"
    - "Use smart pointers"
