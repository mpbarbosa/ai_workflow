# Rust Project Configuration Template
# Auto-generated by AI Workflow Automation
# Version: 1.0.0

_template_info:
  name: "Rust with Cargo"
  description: "Rust project using Cargo for build and dependency management"
  language: "rust"
  build_system: "cargo"
  version: "1.0.0"

project:
  name: "my-rust-project"
  description: "A Rust project"

tech_stack:
  primary_language: "rust"
  languages:
    - rust
  build_system: "cargo"
  test_framework: "cargo test"
  linter: "clippy"
  test_command: "cargo test"
  lint_command: "cargo clippy"

structure:
  source_dirs:
    - src
  test_dirs:
    - tests
  docs_dirs:
    - docs
  exclude_dirs:
    - target

dependencies:
  package_file: "Cargo.toml"
  lock_file: "Cargo.lock"
  install_command: "cargo fetch"

ai_prompts:
  language_context: |
    This is a Rust project using Cargo.
    Focus on Rust safety, ownership, and performance.
  custom_instructions:
    - "Follow Rust API Guidelines"
    - "Document all public APIs"
    - "Use Result for error handling"
    - "Leverage the type system"
